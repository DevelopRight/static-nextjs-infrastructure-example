version: 0.2
env:
  variables:
    S3_BUCKET_ARN: "S3_BUCKET_ARN"
phases:
  build:
    commands:
      - npm ci
      - npm run build && npm run export
      - ls
      - ./scripts/deploy-to-s3.sh ./out $S3_BUCKET_ARN